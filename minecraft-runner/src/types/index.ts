export type PlayRequest = {
    nickname: string;
    uuid: string;
    accessToken: string;
}

export type User = {
    name: string;
    uuid: string;
    access_token: string;
    client_token: string;
}